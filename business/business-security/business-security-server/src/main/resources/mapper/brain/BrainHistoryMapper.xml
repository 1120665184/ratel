<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.quyq.gwsu.security.brain.mapper.BrainHistoryMapper">

    <!-- 查询用户的历史会话列表 -->
    <select id="selectHistorySessions" resultType="org.quyq.gwsu.security.api.brain.vo.BrainHistorySessionVo">
        SELECT s1.session_id                  AS sessionId,
               s1.state_data                  AS title,
               s1.updated_at                  AS updatedAt,
               0                              AS messageCount
        FROM security_brain_sessions s1
        WHERE s1.user_id = #{userId}
          AND s1.state_key = 'agent_state'
          AND s1.item_index = 0
        ORDER BY s1.updated_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 查询用户的历史会话总数 -->
    <select id="countHistorySessions" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM security_brain_sessions
        WHERE user_id = #{userId}
          AND state_key = 'agent_state'
          AND item_index = 0
    </select>

    <!-- 查询会话的消息列表 -->
    <select id="selectSessionMessages">
        SELECT state_data
        FROM security_brain_sessions
        WHERE session_id = #{sessionId}
          AND state_key = 'agent_state'
          AND user_id = #{userId}
        ORDER BY item_index ASC
    </select>

    <!-- 删除会话 -->
    <delete id="deleteSession">
        DELETE
        FROM security_brain_sessions
        WHERE session_id = #{sessionId}
          AND user_id = #{userId}
    </delete>

</mapper>
