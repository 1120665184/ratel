<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.quyq.gwsu.security.tablemodel.mapper.SecurityTableModelColumnMapper">

    <select id="selectByTableId" resultType="org.quyq.gwsu.security.tablemodel.domain.SecurityTableModelColumn">
        SELECT id, table_id, column_name, column_type, column_length, column_scale,
               is_nullable, is_primary_key, pk_position, default_value, column_comment,
               ordinal_position, tenant_id, create_op, create_time, modify_op, modify_time
        FROM security_tablemodel_columns
        WHERE deleted = 0
          AND table_id = #{tableId}
        ORDER BY ordinal_position
    </select>

</mapper>
