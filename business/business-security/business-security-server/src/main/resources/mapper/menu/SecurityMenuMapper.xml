<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.quyq.gwsu.security.menu.mapper.SecurityMenuMapper">

    <!-- 根据主体ID查询菜单列表 -->
    <select id="selectMenusBySubjectId" resultType="org.quyq.gwsu.security.menu.domain.SecurityMenu">
        SELECT DISTINCT m.*
        FROM security_menu m
        INNER JOIN security_role_menu rm ON m.id = rm.menu_id
        INNER JOIN security_role_subject sr ON rm.role_id = sr.role_id
        WHERE sr.subject_id = #{subjectId}
          AND m.owner = #{owner}
          AND m.deleted = 0
          AND m.status = 1
        ORDER BY m.sort ASC
    </select>

    <!-- 根据角色编码列表查询菜单列表 -->
    <select id="selectMenusByRoleCodes" resultType="org.quyq.gwsu.security.menu.domain.SecurityMenu">
        SELECT DISTINCT m.*
        FROM security_menu m
        INNER JOIN security_role_menu rm ON m.id = rm.menu_id
        INNER JOIN security_role r ON rm.role_id = r.id
        WHERE r.role_code IN
        <foreach collection="roleCodes" item="roleCode" open="(" separator="," close=")">
            #{roleCode}
        </foreach>
        AND m.owner = #{owner}
        AND m.deleted = 0
        AND m.status = 1
        ORDER BY m.sort ASC
    </select>

</mapper>
