services:
  gwsu:
    build:
      context: ../..
      dockerfile: docker/single/Dockerfile
    container_name: gwsu
    ports:
      - "${NGINX_PORT:-80}:80"
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME:-gwsu}
      - DB_USERNAME=${DB_USERNAME:-root}
      - DB_PASSWORD=${DB_PASSWORD:-root}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-redis}
      - AI_API_KEY=${AI_API_KEY:-}
      - AI_BASE_URL=${AI_BASE_URL:-}
      - AI_MODEL_NAME=${AI_MODEL_NAME:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - gwsu-net

  postgres:
    image: postgres:16-alpine
    container_name: gwsu-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-gwsu}
      POSTGRES_USER: ${DB_USERNAME:-root}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-root}
    volumes:
      - ../initdb/postgre:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-root}"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - gwsu-net

  redis:
    image: redis:7-alpine
    container_name: gwsu-redis
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis}
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redis}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - redis-data:/data
    networks:
      - gwsu-net

volumes:
  redis-data:

networks:
  gwsu-net:
    driver: bridge
